function sleep(time) {
  return new Promise(function (resolve) {
    return setTimeout(resolve, time);
  });
}

$(".pillar .panel.amb").hover(function() {
  $(this).find("h1, h4, p").toggleClass("invisible");
});