function sleep(time) {
  return new Promise(function (resolve) {
    return setTimeout(resolve, time);
  });
}

$(".pillar .panel.amb").hover(function() {
  $(this).find("h1, h4, p").toggleClass("invisible");
});

Array.prototype.diff = function(a) {
  return this.filter(function(i) {return a.indexOf(i) < 0;});
};

$("#wizard .pillar").hover(function() {
  $(this).find("h1").toggleClass("highlight");
});
